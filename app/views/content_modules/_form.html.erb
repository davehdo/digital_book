<%= form_for(@content_module) do |f| %>
  <% if @content_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_module.errors.count, "error") %> prohibited this content_module from being saved:</h2>

      <ul>
      <% @content_module.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :subtitle %><br />
    <%= f.text_field :subtitle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
